<?php
defined('MOODLE_INTERNAL') || die();

$plugin->component = 'block_monitorias_block';
$plugin->version = 2025052600;       // Fecha actual
$plugin->requires = 2023100900;      // Moodle 4.5 o superior
$plugin->maturity = MATURITY_ALPHA;  // Cambia a STABLE cuando esté probado
$plugin->release = '0.1 alpha';      // Cambia a 1.0 cuando esté listo